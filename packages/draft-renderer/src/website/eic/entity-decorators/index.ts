import { annotationDecorator } from './annotation-decorator'
import { footnoteDecorator } from './footnote-decorator'
import { linkDecorator } from './link-decorator'

export const entityDecorators = {
  annotationDecorator,
  footnoteDecorator,
  linkDecorator,
}
